@model IEnumerable<W.Dev.Models.Sessão>

@{
    ViewBag.Title = "Index";
}

<h2>Programação</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<section id="conteudo">
    <p> </p><br />
            <div class="containerDia_inicorganizador">
                <h2>1° dia (01/10)</h2>
                <div class="containerTurno_inicorganizador">
                    @foreach(var h in Model) { 
                     if(Convert.ToInt32(h.HoraInicial) >= 7 && Convert.ToInt32(h.Horafinal) <= 12){ 
                     <table class="inicorganizador">
                         <tr>
                             <th>
                                 @Html.DisplayNameFor(model => model.Titulo)
                             </th>
                             <th>
                                 @Html.DisplayNameFor(model => model.HoraInicial)
                             </th>
                             <th>
                                 @Html.DisplayNameFor(model => model.Horafinal)
                             </th>
                             <th>
                                 @Html.DisplayNameFor(model => model.Local)
                             </th>
                             <th><a href="paginainicial.html"><img src="editar.png" alt="editar" class="inicorganizador"></a></th>
                             <th></th>
                         </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Titulo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.HoraInicial)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Horafinal)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Local)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.SessãoId }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.SessãoId })
                                </td>
                            </tr>
                            
                        }

                    </table>
                    }
                   }
                    @foreach (var h in Model){
                        if (Convert.ToInt32(h.HoraInicial) >= 12 && Convert.ToInt32(h.Horafinal) <= 19)
                        {

                    <table class="table">
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Titulo)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.HoraInicial)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Horafinal)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Local)
                            </th>
                            <th></th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Titulo)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.HoraInicial)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Horafinal)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Local)
                                </td>
                                <td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.SessãoId }) |
                                    @Html.ActionLink("Delete", "Delete", new { id = item.SessãoId })
                                </td>
                            </tr>
                        }

                    </table>
                        }
                    }
                </div>
                </div>
</section>